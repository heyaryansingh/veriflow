---
phase: 03-classic-ml-evaluation
plan: 02
type: execute
depends_on: ["03-01"]
files_modified: [veriflow/evaluation/deterministic.py]
---

<objective>
Implement deterministic evaluation setup with seed management.

Purpose: Ensure ML evaluations are reproducible by managing random seeds and providing utilities for deterministic model evaluation. This is critical for comparing evaluations across runs.
Output: Deterministic evaluation utilities that set seeds and ensure reproducible evaluation runs.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classic-ml-evaluation/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deterministic evaluation utilities</name>
  <files>veriflow/evaluation/deterministic.py</files>
  <action>Create `veriflow/evaluation/deterministic.py` with:
- `set_deterministic_seed(seed: int = 42) -> None`: Sets seeds for numpy, random, and Python's random module
- `get_evaluation_seed(config_seed: int | None = None) -> int`: Gets evaluation seed from config or defaults to 42
- `ensure_deterministic() -> None`: Context manager or function that ensures deterministic behavior during evaluation
- Handle multiple random number generators (numpy.random, random, torch if available)
- Export functions from `veriflow/evaluation/__init__.py`</action>
  <verify>File exists, seed setting works correctly, deterministic behavior verified</verify>
  <done>Deterministic evaluation utilities created</done>
</task>

<task type="auto">
  <name>Task 2: Add seed configuration to schema</name>
  <files>veriflow/schema.py</files>
  <action>Update `EvaluationConfig` in `veriflow/schema.py`:
- Add `seed: int | None = Field(default=42, description="Random seed for deterministic evaluation")`
- This allows users to configure seed in veriflow.yaml
- Default to 42 if not specified</action>
  <verify>Schema updated, seed field added to EvaluationConfig</verify>
  <done>Seed configuration added to schema</done>
</task>

<task type="auto">
  <name>Task 3: Test deterministic behavior</name>
  <files></files>
  <action>Test deterministic evaluation:
- Set seed to 42, generate random numbers, verify same sequence
- Run evaluation twice with same seed, verify identical results
- Test with different seeds, verify different results
- Verify seed persists across function calls during evaluation
- Clean up test data after verification</action>
  <verify>Deterministic behavior verified, same seed produces same results</verify>
  <done>Deterministic behavior tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/evaluation/deterministic.py` exists with seed management functions
- [ ] `EvaluationConfig` includes seed field
- [ ] Deterministic behavior verified with tests
- [ ] Functions exported from `veriflow/evaluation/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Deterministic evaluation utilities implemented
- Seed configuration added to schema
- Deterministic behavior verified

</success_criteria>

<output>
After completion, create `.planning/phases/03-classic-ml-evaluation/03-02-SUMMARY.md`:

# Phase 3 Plan 2: Deterministic Evaluation Summary

**Implemented deterministic evaluation setup with seed management**

## Accomplishments

- Created deterministic evaluation utilities with seed management
- Added seed configuration to EvaluationConfig schema
- Verified deterministic behavior

## Files Created/Modified

- `veriflow/evaluation/deterministic.py` - Deterministic evaluation utilities
- `veriflow/schema.py` - Added seed field to EvaluationConfig
- `veriflow/evaluation/__init__.py` - Added deterministic exports

## Decisions Made

- Default seed: 42 (standard ML default)
- Seed management: Sets numpy, random, and Python random seeds
- Configuration: Seed configurable via veriflow.yaml

## Issues Encountered

None

## Next Step

Ready for 03-03-PLAN.md (Bootstrap confidence intervals)

</output>
