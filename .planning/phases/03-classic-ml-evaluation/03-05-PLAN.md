---
phase: 03-classic-ml-evaluation
plan: 05
type: execute
depends_on: ["03-04"]
files_modified: [veriflow/evaluation/comparison.py]
---

<objective>
Implement paired comparison vs baseline.

Purpose: Compare current evaluation results against baseline to detect regressions and improvements. Uses statistical comparison of metrics with bootstrap confidence intervals.
Output: Comparison module that compares current and baseline results, detecting significant changes.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classic-ml-evaluation/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comparison module</name>
  <files>veriflow/evaluation/comparison.py</files>
  <action>Create `veriflow/evaluation/comparison.py` with:
- `compare_results(current: EvaluationResult, baseline: EvaluationResult) -> dict`: Compares two evaluation results:
  - Computes deltas for each metric (current - baseline)
  - Checks if CIs overlap (no significant change) or don't overlap (significant change)
  - Returns dict with: passed (bool), comparisons (list of dicts with metric name, delta, significant, details)
- `is_significant_change(current_ci: dict, baseline_ci: dict) -> bool`: Checks if CIs don't overlap (significant change)
- `compute_metric_delta(current_value: float, baseline_value: float) -> dict`: Computes delta and relative change
- Export functions from `veriflow/evaluation/__init__.py`</action>
  <verify>File exists, comparison works correctly, significant changes detected</verify>
  <done>Comparison module created</done>
</task>

<task type="auto">
  <name>Task 2: Add baseline comparison function</name>
  <files>veriflow/evaluation/comparison.py</files>
  <action>Add `compare_vs_baseline(current_path: Path, baseline_path: Path) -> dict`:
- Loads current and baseline results from JSON files
- Calls compare_results to compare them
- Returns comparison dict with pass/fail status and details
- Handles missing files gracefully
- Clear error messages if comparison fails</action>
  <verify>Function exists, loads results and compares correctly</verify>
  <done>Baseline comparison function implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test comparison functionality</name>
  <files></files>
  <action>Test comparison:
- Create baseline result with known metrics
- Create current result with improved metrics (should detect improvement)
- Create current result with regressed metrics (should detect regression)
- Create current result with similar metrics (should detect no significant change)
- Test CI overlap detection (overlapping CIs = no significant change)
- Clean up test files after verification</action>
  <verify>Comparison detects improvements, regressions, and no-change cases correctly</verify>
  <done>Comparison functionality tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/evaluation/comparison.py` exists with comparison functions
- [ ] Comparison detects significant changes correctly
- [ ] Baseline comparison function works
- [ ] CI overlap detection works correctly
- [ ] Functions exported from `veriflow/evaluation/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Paired comparison implemented
- Significant change detection works
- Baseline comparison function works
- Comparison tested with various scenarios

</success_criteria>

<output>
After completion, create `.planning/phases/03-classic-ml-evaluation/03-05-SUMMARY.md`:

# Phase 3 Plan 5: Paired Comparison Summary

**Implemented paired comparison vs baseline**

## Accomplishments

- Created comparison module for comparing evaluation results
- Implemented significant change detection using CI overlap
- Added baseline comparison function

## Files Created/Modified

- `veriflow/evaluation/comparison.py` - Comparison implementation
- `veriflow/evaluation/__init__.py` - Added comparison exports

## Decisions Made

- Significant change: CIs don't overlap (statistical significance)
- Comparison: Computes deltas and relative changes for all metrics
- Baseline comparison: Loads results from JSON files and compares

## Issues Encountered

None

## Next Step

Ready for 03-06-PLAN.md (Integration with CLI and config)

</output>
