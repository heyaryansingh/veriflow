---
phase: 03-classic-ml-evaluation
plan: 04
type: execute
depends_on: ["03-01", "03-03"]
files_modified: [veriflow/evaluation/results.py]
---

<objective>
Implement evaluation result storage and loading.

Purpose: Store evaluation results (metrics, bootstrap CIs, metadata) for comparison with baseline. Results are stored in JSON format for easy inspection and comparison.
Output: Result storage module that saves and loads evaluation results with metadata.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classic-ml-evaluation/03-01-SUMMARY.md
@.planning/phases/03-classic-ml-evaluation/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create evaluation result storage module</name>
  <files>veriflow/evaluation/results.py</files>
  <action>Create `veriflow/evaluation/results.py` with:
- `EvaluationResult` dataclass: Contains metrics (dict), bootstrap_cis (dict), metadata (dict), timestamp (datetime)
- `save_evaluation_result(result: EvaluationResult, path: Path) -> None`: Saves result to JSON file in `artifacts/` directory
- `load_evaluation_result(path: Path) -> EvaluationResult | None`: Loads result from JSON, returns None if not found
- `result_to_dict(result: EvaluationResult) -> dict`: Converts to dict for JSON serialization
- `result_from_dict(data: dict) -> EvaluationResult`: Creates from dict
- Handle JSON serialization (datetime, numpy types)
- Export functions from `veriflow/evaluation/__init__.py`</action>
  <verify>File exists, results save/load correctly, JSON serialization works</verify>
  <done>Evaluation result storage module created</done>
</task>

<task type="auto">
  <name>Task 2: Add result computation function</name>
  <files>veriflow/evaluation/results.py</files>
  <action>Add `compute_evaluation_result(y_true, y_pred, y_scores=None, metrics=None, n_bootstrap=1000, seed=None) -> EvaluationResult`:
- Computes all metrics (accuracy, F1, ROC-AUC, calibration if y_scores provided)
- Computes bootstrap CIs for all metrics
- Creates EvaluationResult with metrics, CIs, and metadata (n_samples, timestamp, seed)
- Uses functions from metrics.py and bootstrap.py
- Returns complete evaluation result ready for storage</action>
  <verify>Function exists, computes complete evaluation result correctly</verify>
  <done>Result computation function implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test result storage and loading</name>
  <files></files>
  <action>Test evaluation result storage:
- Create test evaluation result with metrics and CIs
- Save result to JSON file
- Load result from JSON file
- Verify all data preserved (metrics, CIs, metadata)
- Test with different metric combinations
- Clean up test files after verification</action>
  <verify>Results save/load correctly, all data preserved, JSON format is readable</verify>
  <done>Result storage and loading tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/evaluation/results.py` exists with result storage functions
- [ ] EvaluationResult dataclass defined
- [ ] Results save/load correctly
- [ ] Result computation function works
- [ ] Functions exported from `veriflow/evaluation/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Evaluation result storage implemented
- Result computation function works
- Results save/load correctly
- JSON format is readable

</success_criteria>

<output>
After completion, create `.planning/phases/03-classic-ml-evaluation/03-04-SUMMARY.md`:

# Phase 3 Plan 4: Evaluation Result Storage Summary

**Implemented evaluation result storage and loading**

## Accomplishments

- Created EvaluationResult dataclass for storing evaluation results
- Implemented result save/load functions
- Added result computation function that combines metrics and bootstrap CIs

## Files Created/Modified

- `veriflow/evaluation/results.py` - Result storage implementation
- `veriflow/evaluation/__init__.py` - Added result exports

## Decisions Made

- Storage format: JSON files in artifacts/ directory
- Result structure: Metrics, bootstrap CIs, metadata, timestamp
- Serialization: Handle datetime and numpy types for JSON

## Issues Encountered

None

## Next Step

Ready for 03-05-PLAN.md (Paired comparison vs baseline)

</output>
