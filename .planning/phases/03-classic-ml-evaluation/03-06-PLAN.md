---
phase: 03-classic-ml-evaluation
plan: 06
type: execute
depends_on: ["03-02", "03-04", "03-05"]
files_modified: [veriflow/evaluation/evaluator.py, veriflow/cli.py]
---

<objective>
Integrate ML evaluation into CLI and config system.

Purpose: Wire up ML evaluation (metrics, bootstrap CIs, comparison) into a unified evaluation system accessible via CLI. This completes Phase 3 by making ML evaluation executable.
Output: Integrated evaluation module that runs ML evaluation based on config, accessible via `veriflow run`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classic-ml-evaluation/03-02-SUMMARY.md
@.planning/phases/03-classic-ml-evaluation/03-04-SUMMARY.md
@.planning/phases/03-classic-ml-evaluation/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create evaluation orchestrator</name>
  <files>veriflow/evaluation/evaluator.py</files>
  <action>Create `veriflow/evaluation/evaluator.py` with:
- `run_ml_evaluation(config: VeriflowConfig, y_true, y_pred, y_scores=None) -> dict`: Orchestrates ML evaluation:
  - Sets deterministic seed from config
  - Computes evaluation result (metrics + bootstrap CIs)
  - Saves result to artifacts/ directory
  - If baseline exists: compares against baseline
  - Returns dict with: passed (bool), result (EvaluationResult), comparison (dict if baseline exists), summary (str)
- Handles missing data, config errors gracefully
- Export from `veriflow/evaluation/__init__.py`</action>
  <verify>File exists, evaluation orchestrator works, integrates all components</verify>
  <done>Evaluation orchestrator created</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI run command to execute ML evaluation</name>
  <files>veriflow/cli.py</files>
  <action>Update `veriflow/cli.py` `run()` command:
- Check if config.model is configured (indicates ML evaluation needed)
- If model config exists: Look for evaluation data (y_true, y_pred files or user-provided paths)
- Call `run_ml_evaluation()` with config and data
- Print results: metrics, bootstrap CIs, comparison vs baseline if available
- Exit with non-zero code if evaluation fails or regressions detected
- Keep existing data checks integration
- Handle case where ML evaluation not configured</action>
  <verify>Updated CLI, `veriflow run` executes ML evaluation, prints results clearly</verify>
  <done>CLI run command integrated with ML evaluation</done>
</task>

<task type="auto">
  <name>Task 3: Test end-to-end ML evaluation flow</name>
  <files></files>
  <action>Test complete ML evaluation flow:
- Create test veriflow.yaml with model and evaluation config
- Create test evaluation data (y_true, y_pred, y_scores)
- Run `veriflow run` - should compute metrics and CIs
- Create baseline result, run again - should compare against baseline
- Test with regression (worse metrics) - should detect and fail
- Test with improvement (better metrics) - should pass
- Verify error messages are clear and actionable
- Clean up test files after verification</action>
  <verify>End-to-end flow works, metrics computed, comparison works, CLI integration works</verify>
  <done>ML evaluation flow tested end-to-end</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/evaluation/evaluator.py` exists with evaluation orchestrator
- [ ] CLI run command executes ML evaluation
- [ ] Config controls evaluation execution
- [ ] Results printed clearly with metrics and CIs
- [ ] End-to-end flow works with test data
</verification>

<success_criteria>

- All tasks completed
- Evaluation orchestrator functional
- CLI run command integrated
- Config controls evaluation execution
- End-to-end evaluation flow works

</success_criteria>

<output>
After completion, create `.planning/phases/03-classic-ml-evaluation/03-06-SUMMARY.md`:

# Phase 3 Plan 6: ML Evaluation Integration Summary

**Integrated ML evaluation into CLI and config system**

## Accomplishments

- Created evaluation orchestrator
- Updated CLI run command to execute ML evaluation
- Tested end-to-end evaluation flow

## Files Created/Modified

- `veriflow/evaluation/evaluator.py` - Evaluation orchestrator
- `veriflow/cli.py` - Updated run command

## Decisions Made

- Evaluation orchestration: Single function that runs all evaluation steps
- Config-driven: Evaluation enabled via config.model configuration
- Integration: ML evaluation runs alongside data checks in `veriflow run`

## Issues Encountered

None

## Next Step

Phase 3 complete. Ready for Phase 4: LLM/RAG Evaluation

</output>
