---
phase: 03-classic-ml-evaluation
plan: 01
type: execute
depends_on: []
files_modified: [veriflow/evaluation/__init__.py, veriflow/evaluation/metrics.py]
---

<objective>
Implement ML metrics computation (accuracy, F1, ROC-AUC, calibration).

Purpose: Create core metrics computation functions for classic ML evaluation. These metrics are standard and well-established, forming the foundation for evaluation comparisons.
Output: Metrics module with functions to compute accuracy, F1, ROC-AUC, and calibration metrics from predictions and ground truth.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create metrics computation module</name>
  <files>veriflow/evaluation/__init__.py, veriflow/evaluation/metrics.py</files>
  <action>Create `veriflow/evaluation/` directory and `veriflow/evaluation/metrics.py` module with:
- `compute_accuracy(y_true, y_pred) -> float`: Computes accuracy score
- `compute_f1(y_true, y_pred, average='binary' | 'macro' | 'micro' | 'weighted') -> float`: Computes F1 score with averaging options
- `compute_roc_auc(y_true, y_scores, multi_class='raise' | 'ovr' | 'ovo') -> float`: Computes ROC-AUC score (handles binary and multiclass)
- `compute_calibration_ece(y_true, y_probs, n_bins=10) -> float`: Computes Expected Calibration Error (ECE)
- Use sklearn.metrics for implementation (accuracy_score, f1_score, roc_auc_score)
- For calibration, implement ECE calculation manually (bin probabilities, compute calibration error)
- Handle edge cases (empty arrays, single class, etc.)
- Export functions from `veriflow/evaluation/__init__.py`</action>
  <verify>File exists, can import functions, metrics compute correctly on test data</verify>
  <done>Metrics computation module created</done>
</task>

<task type="auto">
  <name>Task 2: Add scikit-learn dependency</name>
  <files>pyproject.toml</files>
  <action>Update `pyproject.toml` to add scikit-learn dependency:
- scikit-learn>=1.3.0 (for metrics computation)
- Add to main dependencies list (not optional)
- This is a core requirement for ML evaluation</action>
  <verify>File updated, scikit-learn added to dependencies</verify>
  <done>Scikit-learn dependency added</done>
</task>

<task type="auto">
  <name>Task 3: Test metrics computation</name>
  <files></files>
  <action>Test metrics computation:
- Create test data (binary classification): y_true, y_pred, y_probs
- Test accuracy: should compute correctly
- Test F1: test binary and multiclass cases
- Test ROC-AUC: test binary and multiclass cases
- Test calibration ECE: test with well-calibrated and poorly-calibrated predictions
- Verify edge cases handled (empty arrays, single class)
- Install dependencies: `pip install -e .`
- Clean up test data after verification</action>
  <verify>All metrics compute correctly, edge cases handled gracefully</verify>
  <done>Metrics computation tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/evaluation/metrics.py` exists with all metric functions
- [ ] `pyproject.toml` includes scikit-learn dependency
- [ ] All metrics compute correctly on test data
- [ ] Edge cases handled gracefully
- [ ] Functions exported from `veriflow/evaluation/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- ML metrics computation implemented
- Accuracy, F1, ROC-AUC, calibration metrics work correctly
- Edge cases handled
- Dependencies installed

</success_criteria>

<output>
After completion, create `.planning/phases/03-classic-ml-evaluation/03-01-SUMMARY.md`:

# Phase 3 Plan 1: ML Metrics Computation Summary

**Implemented ML metrics computation (accuracy, F1, ROC-AUC, calibration)**

## Accomplishments

- Created metrics computation module with accuracy, F1, ROC-AUC, and calibration functions
- Added scikit-learn dependency
- Implemented Expected Calibration Error (ECE) calculation

## Files Created/Modified

- `veriflow/evaluation/__init__.py` - Evaluation module initialization
- `veriflow/evaluation/metrics.py` - Metrics computation implementation
- `pyproject.toml` - Added scikit-learn dependency

## Decisions Made

- Metrics library: scikit-learn (standard, well-tested)
- Calibration metric: Expected Calibration Error (ECE)
- Averaging: Support binary, macro, micro, weighted for F1

## Issues Encountered

None

## Next Step

Ready for 03-02-PLAN.md (Deterministic evaluation setup)

</output>
