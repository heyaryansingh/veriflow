---
phase: 03-classic-ml-evaluation
plan: 03
type: execute
depends_on: ["03-01"]
files_modified: [veriflow/evaluation/bootstrap.py]
---

<objective>
Implement bootstrap confidence intervals for ML metrics.

Purpose: Compute confidence intervals for ML metrics using bootstrap resampling. This provides statistical rigor for metric comparisons and regression detection.
Output: Bootstrap confidence interval computation module that resamples data and computes confidence intervals for metrics.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-classic-ml-evaluation/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bootstrap confidence interval module</name>
  <files>veriflow/evaluation/bootstrap.py</files>
  <action>Create `veriflow/evaluation/bootstrap.py` with:
- `bootstrap_metric(y_true, y_pred, metric_func, n_bootstrap=1000, confidence=0.95, seed=None) -> dict`: Computes bootstrap confidence interval for a metric:
  - Resamples data n_bootstrap times with replacement
  - Computes metric on each resample
  - Returns dict with: metric_value (float), ci_lower (float), ci_upper (float), confidence (float)
- `bootstrap_metrics(y_true, y_pred, metrics_dict, n_bootstrap=1000, confidence=0.95, seed=None) -> dict`: Computes bootstrap CIs for multiple metrics
- Use numpy for resampling, ensure deterministic with seed
- Handle edge cases (small datasets, single class)
- Export functions from `veriflow/evaluation/__init__.py`</action>
  <verify>File exists, bootstrap CIs computed correctly, handles edge cases</verify>
  <done>Bootstrap confidence interval module created</done>
</task>

<task type="auto">
  <name>Task 2: Integrate bootstrap with metrics</name>
  <files>veriflow/evaluation/bootstrap.py</files>
  <action>Add convenience functions that combine metrics and bootstrap:
- `bootstrap_accuracy(y_true, y_pred, n_bootstrap=1000, confidence=0.95, seed=None) -> dict`
- `bootstrap_f1(y_true, y_pred, n_bootstrap=1000, confidence=0.95, seed=None) -> dict`
- `bootstrap_roc_auc(y_true, y_scores, n_bootstrap=1000, confidence=0.95, seed=None) -> dict`
- These wrap metric computation with bootstrap CI calculation
- Use functions from metrics.py</action>
  <verify>Convenience functions exist, integrate correctly with metrics module</verify>
  <done>Bootstrap integrated with metrics</done>
</task>

<task type="auto">
  <name>Task 3: Test bootstrap confidence intervals</name>
  <files></files>
  <action>Test bootstrap CI computation:
- Create test data (binary classification)
- Compute bootstrap CI for accuracy, verify CI bounds are reasonable
- Test with different confidence levels (0.90, 0.95, 0.99)
- Test with different bootstrap sample sizes (100, 1000)
- Verify deterministic behavior with seed
- Test edge cases (small dataset, single class)
- Clean up test data after verification</action>
  <verify>Bootstrap CIs computed correctly, confidence levels work, deterministic behavior verified</verify>
  <done>Bootstrap confidence intervals tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/evaluation/bootstrap.py` exists with bootstrap CI functions
- [ ] Bootstrap CIs computed correctly
- [ ] Convenience functions integrate with metrics
- [ ] Edge cases handled gracefully
- [ ] Functions exported from `veriflow/evaluation/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Bootstrap confidence intervals implemented
- Integration with metrics module works
- Edge cases handled
- Deterministic behavior verified

</success_criteria>

<output>
After completion, create `.planning/phases/03-classic-ml-evaluation/03-03-SUMMARY.md`:

# Phase 3 Plan 3: Bootstrap Confidence Intervals Summary

**Implemented bootstrap confidence intervals for ML metrics**

## Accomplishments

- Created bootstrap confidence interval computation module
- Integrated bootstrap with metrics module
- Added convenience functions for common metrics

## Files Created/Modified

- `veriflow/evaluation/bootstrap.py` - Bootstrap CI implementation
- `veriflow/evaluation/__init__.py` - Added bootstrap exports

## Decisions Made

- Bootstrap method: Resample with replacement, compute metric, use percentiles for CI
- Default bootstrap samples: 1000
- Default confidence: 0.95 (95% CI)

## Issues Encountered

None

## Next Step

Ready for 03-04-PLAN.md (Evaluation result storage)

</output>
