---
phase: 04-llm-rag-evaluation
plan: 03
type: execute
depends_on: ["04-02"]
files_modified: [veriflow/llm/judge.py]
---

<objective>
Implement judge model support for LLM evaluation.

Purpose: Integrate judge models (LLMs that evaluate other LLM outputs) for automated evaluation. Judge models can assess quality, correctness, and adherence to criteria without requiring ground truth labels.
Output: Judge model integration module that calls judge models and processes their evaluations.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-llm-rag-evaluation/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create judge model integration module</name>
  <files>veriflow/llm/judge.py</files>
  <action>Create `veriflow/llm/judge.py` with:
- `JudgeModel` class: Wraps judge model configuration (model_name, api_key, temperature, etc.)
- `evaluate_with_judge(judge: JudgeModel, input: str, output: str, criteria: str | None = None) -> dict`: Calls judge model to evaluate LLM output:
  - Constructs evaluation prompt with input, output, and criteria
  - Calls judge model API (OpenAI, Anthropic, or custom)
  - Returns dict with: score (float), reasoning (str), passed (bool), raw_response (str)
- `batch_evaluate_with_judge(judge: JudgeModel, test_cases: list[dict]) -> list[dict]`: Evaluates multiple outputs
- Support OpenAI and Anthropic APIs (via their Python SDKs)
- Handle API errors, rate limits gracefully
- Export functions from `veriflow/llm/__init__.py`</action>
  <verify>File exists, judge model integration works, handles API errors gracefully</verify>
  <done>Judge model integration module created</done>
</task>

<task type="auto">
  <name>Task 2: Add judge model configuration to schema</name>
  <files>veriflow/schema.py</files>
  <action>Update schema to support judge model configuration:
- Add `JudgeModelConfig` class: model_name (str), provider (Literal["openai", "anthropic", "custom"]), api_key_env (str), temperature (float), max_tokens (int)
- Add `judge_model: JudgeModelConfig | None` to `EvaluationConfig`
- This allows users to configure judge models in veriflow.yaml
- Export from schema module</action>
  <verify>Schema updated, judge model config added to EvaluationConfig</verify>
  <done>Judge model configuration added to schema</done>
</task>

<task type="auto">
  <name>Task 3: Test judge model integration</name>
  <files></files>
  <action>Test judge model integration:
- Create mock judge model function (simulates API call)
- Test evaluate_with_judge with mock judge, verify evaluation format
- Test batch evaluation, verify all test cases evaluated
- Test error handling (API errors, rate limits)
- Test with different criteria, verify criteria affects evaluation
- Clean up test data after verification</action>
  <verify>Judge model integration works, batch evaluation works, error handling works</verify>
  <done>Judge model integration tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/llm/judge.py` exists with judge model integration
- [ ] Judge model configuration added to schema
- [ ] Judge model evaluation works correctly
- [ ] Error handling works gracefully
- [ ] Functions exported from `veriflow/llm/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Judge model integration implemented
- Schema updated with judge model config
- Judge model evaluation works
- Error handling tested

</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-rag-evaluation/04-03-SUMMARY.md`:

# Phase 4 Plan 3: Judge Model Support Summary

**Implemented judge model support for LLM evaluation**

## Accomplishments

- Created judge model integration module
- Added judge model configuration to schema
- Implemented judge model evaluation with API support

## Files Created/Modified

- `veriflow/llm/judge.py` - Judge model integration
- `veriflow/schema.py` - Added JudgeModelConfig

## Decisions Made

- Judge providers: OpenAI, Anthropic, custom (extensible)
- Evaluation format: Score, reasoning, passed boolean
- API handling: Rate limiting, error handling, retry logic

## Issues Encountered

None

## Next Step

Ready for 04-04-PLAN.md (Structured output validation)

</output>
