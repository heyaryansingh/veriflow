---
phase: 04-llm-rag-evaluation
plan: 06
type: execute
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified: [veriflow/llm/evaluator.py, veriflow/cli.py]
---

<objective>
Integrate LLM/RAG evaluation into CLI and config system.

Purpose: Wire up all LLM evaluation components (prompt hashing, dataset replay, judge models, structured output validation, human annotation hooks) into a unified evaluation system accessible via CLI. This completes Phase 4 by making LLM evaluation executable.
Output: Integrated LLM evaluation module that runs LLM evaluation based on config, accessible via `veriflow run`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-llm-rag-evaluation/04-01-SUMMARY.md
@.planning/phases/04-llm-rag-evaluation/04-02-SUMMARY.md
@.planning/phases/04-llm-rag-evaluation/04-03-SUMMARY.md
@.planning/phases/04-llm-rag-evaluation/04-04-SUMMARY.md
@.planning/phases/04-llm-rag-evaluation/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LLM evaluation orchestrator</name>
  <files>veriflow/llm/evaluator.py</files>
  <action>Create `veriflow/llm/evaluator.py` with:
- `run_llm_evaluation(config: VeriflowConfig, llm_func: Callable, dataset_path: Path) -> dict`: Orchestrates LLM evaluation:
  - Loads evaluation dataset
  - Hashes prompts (if prompt provided)
  - Replays dataset through LLM function
  - Validates structured outputs (if schema provided)
  - Calls judge model (if configured)
  - Collects human annotations (if enabled)
  - Returns dict with: passed (bool), results (list), summary (str)
- Handles missing data, config errors gracefully
- Export from `veriflow/llm/__init__.py`</action>
  <verify>File exists, LLM evaluation orchestrator works, integrates all components</verify>
  <done>LLM evaluation orchestrator created</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI run command to execute LLM evaluation</name>
  <files>veriflow/cli.py</files>
  <action>Update `veriflow/cli.py` `run()` command:
- Check if config.project.type is "llm_classification" or "rag" (indicates LLM evaluation needed)
- If LLM project: Look for evaluation dataset (from config or default location)
- Call `run_llm_evaluation()` with config, LLM function, and dataset
- Print results: prompt hash, evaluation results, judge scores, validation results
- Exit with non-zero code if evaluation fails
- Keep existing data checks and ML evaluation integration
- Handle case where LLM evaluation not configured</action>
  <verify>Updated CLI, `veriflow run` executes LLM evaluation, prints results clearly</verify>
  <done>CLI run command integrated with LLM evaluation</done>
</task>

<task type="auto">
  <name>Task 3: Test end-to-end LLM evaluation flow</name>
  <files></files>
  <action>Test complete LLM evaluation flow:
- Create test veriflow.yaml with LLM project config
- Create test evaluation dataset (JSON format)
- Create mock LLM function
- Run `veriflow run` - should execute LLM evaluation
- Test with judge model (mock), verify judge scores included
- Test with structured output validation, verify validation works
- Verify error messages are clear and actionable
- Clean up test files after verification</action>
  <verify>End-to-end flow works, LLM evaluation executes, all components integrated</verify>
  <done>LLM evaluation flow tested end-to-end</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/llm/evaluator.py` exists with LLM evaluation orchestrator
- [ ] CLI run command executes LLM evaluation
- [ ] Config controls LLM evaluation execution
- [ ] Results printed clearly with prompt hash, evaluation results
- [ ] End-to-end flow works with test data
</verification>

<success_criteria>

- All tasks completed
- LLM evaluation orchestrator functional
- CLI run command integrated
- Config controls LLM evaluation execution
- End-to-end LLM evaluation flow works

</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-rag-evaluation/04-06-SUMMARY.md`:

# Phase 4 Plan 6: LLM Evaluation Integration Summary

**Integrated LLM/RAG evaluation into CLI and config system**

## Accomplishments

- Created LLM evaluation orchestrator
- Updated CLI run command to execute LLM evaluation
- Tested end-to-end LLM evaluation flow

## Files Created/Modified

- `veriflow/llm/evaluator.py` - LLM evaluation orchestrator
- `veriflow/cli.py` - Updated run command

## Decisions Made

- Evaluation orchestration: Single function that runs all LLM evaluation steps
- Config-driven: LLM evaluation enabled via config.project.type
- Integration: LLM evaluation runs alongside data checks and ML evaluation in `veriflow run`

## Issues Encountered

None

## Next Step

Phase 4 complete. Ready for Phase 5: Regression Gates

</output>
