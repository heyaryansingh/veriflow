---
phase: 04-llm-rag-evaluation
plan: 04
type: execute
depends_on: []
files_modified: [veriflow/llm/structured_output.py]
---

<objective>
Implement structured output validation for LLM responses.

Purpose: Validate that LLM outputs conform to expected JSON schemas or structures. This catches when LLMs produce malformed JSON or violate expected output contracts.
Output: Structured output validation module that validates LLM outputs against JSON schemas.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structured output validation module</name>
  <files>veriflow/llm/structured_output.py</files>
  <action>Create `veriflow/llm/structured_output.py` with:
- `validate_json_output(output: str, schema: dict | None = None) -> dict`: Validates JSON output:
  - Parses JSON from string
  - If schema provided: validates against JSON schema using jsonschema library
  - Returns dict with: valid (bool), parsed_json (dict | None), errors (list[str])
- `validate_structured_output(output: str, expected_keys: list[str] | None = None) -> dict`: Validates structure:
  - Parses JSON
  - Checks for expected keys if provided
  - Returns validation result
- Handle JSON parsing errors, schema validation errors gracefully
- Export functions from `veriflow/llm/__init__.py`</action>
  <verify>File exists, JSON validation works, schema validation works</verify>
  <done>Structured output validation module created</done>
</task>

<task type="auto">
  <name>Task 2: Add jsonschema dependency</name>
  <files>pyproject.toml</files>
  <action>Update `pyproject.toml` to add jsonschema dependency:
- jsonschema>=4.17.0 (for JSON schema validation)
- Add to main dependencies list (not optional)
- This is a core requirement for structured output validation</action>
  <verify>File updated, jsonschema added to dependencies</verify>
  <done>Jsonschema dependency added</done>
</task>

<task type="auto">
  <name>Task 3: Test structured output validation</name>
  <files></files>
  <action>Test structured output validation:
- Create test JSON strings (valid and invalid)
- Test JSON parsing, verify valid JSON parsed correctly
- Test schema validation with JSON schema, verify validation works
- Test with expected_keys, verify key checking works
- Test error handling (malformed JSON, schema violations)
- Install dependencies: `pip install -e .`
- Clean up test data after verification</action>
  <verify>Structured output validation works correctly, schema validation works, error handling works</verify>
  <done>Structured output validation tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/llm/structured_output.py` exists with validation functions
- [ ] `pyproject.toml` includes jsonschema dependency
- [ ] JSON validation works correctly
- [ ] Schema validation works correctly
- [ ] Functions exported from `veriflow/llm/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Structured output validation implemented
- JSON schema validation works
- Error handling works
- Dependencies installed

</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-rag-evaluation/04-04-SUMMARY.md`:

# Phase 4 Plan 4: Structured Output Validation Summary

**Implemented structured output validation for LLM responses**

## Accomplishments

- Created structured output validation module
- Added jsonschema dependency
- Implemented JSON schema validation

## Files Created/Modified

- `veriflow/llm/structured_output.py` - Structured output validation
- `pyproject.toml` - Added jsonschema dependency
- `veriflow/llm/__init__.py` - Added validation exports

## Decisions Made

- Validation library: jsonschema (standard JSON schema validation)
- Validation: JSON parsing + schema validation
- Error handling: Clear error messages for validation failures

## Issues Encountered

None

## Next Step

Ready for 04-05-PLAN.md (Human annotation hooks)

</output>
