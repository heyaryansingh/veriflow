---
phase: 04-llm-rag-evaluation
plan: 05
type: execute
depends_on: ["04-02"]
files_modified: [veriflow/llm/human_annotation.py]
---

<objective>
Implement human annotation hooks for LLM evaluation.

Purpose: Provide optional hooks for human evaluation of LLM outputs. This allows users to integrate human-in-the-loop evaluation workflows when automated evaluation is insufficient.
Output: Human annotation hooks module that supports collecting and integrating human evaluations.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-llm-rag-evaluation/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create human annotation hooks module</name>
  <files>veriflow/llm/human_annotation.py</files>
  <action>Create `veriflow/llm/human_annotation.py` with:
- `HumanAnnotation` dataclass: Contains test_case_id (str), annotator (str), score (float), notes (str), timestamp (datetime)
- `save_human_annotation(annotation: HumanAnnotation, path: Path) -> None`: Saves annotation to JSON file
- `load_human_annotations(path: Path) -> list[HumanAnnotation]`: Loads annotations from JSON file
- `collect_human_annotations(test_cases: list[dict], output_dir: Path) -> None`: Creates annotation files for human review:
  - Creates JSON files with test cases and LLM outputs
  - Provides template for human annotators
  - Saves to output directory
- `integrate_human_annotations(annotations: list[HumanAnnotation], evaluation_results: list[dict]) -> list[dict]`: Integrates human annotations with evaluation results
- Export functions from `veriflow/llm/__init__.py`</action>
  <verify>File exists, annotation hooks work, integration works</verify>
  <done>Human annotation hooks module created</done>
</task>

<task type="auto">
  <name>Task 2: Add human annotation configuration to schema</name>
  <files>veriflow/schema.py</files>
  <action>Update schema to support human annotation configuration:
- Add `HumanAnnotationConfig` class: enabled (bool), output_dir (str), required (bool)
- Add `human_annotation: HumanAnnotationConfig | None` to `EvaluationConfig`
- This allows users to enable/configure human annotation in veriflow.yaml
- Export from schema module</action>
  <verify>Schema updated, human annotation config added to EvaluationConfig</verify>
  <done>Human annotation configuration added to schema</done>
</task>

<task type="auto">
  <name>Task 3: Test human annotation hooks</name>
  <files></files>
  <action>Test human annotation hooks:
- Create test evaluation results
- Test collect_human_annotations, verify annotation files created
- Create test human annotations, test integration with evaluation results
- Test save/load annotations, verify data preserved
- Clean up test files after verification</action>
  <verify>Human annotation hooks work, integration works, save/load works</verify>
  <done>Human annotation hooks tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/llm/human_annotation.py` exists with annotation hooks
- [ ] Human annotation configuration added to schema
- [ ] Annotation collection works
- [ ] Annotation integration works
- [ ] Functions exported from `veriflow/llm/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Human annotation hooks implemented
- Schema updated with human annotation config
- Annotation collection and integration work
- Hooks tested

</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-rag-evaluation/04-05-SUMMARY.md`:

# Phase 4 Plan 5: Human Annotation Hooks Summary

**Implemented human annotation hooks for LLM evaluation**

## Accomplishments

- Created human annotation hooks module
- Added human annotation configuration to schema
- Implemented annotation collection and integration

## Files Created/Modified

- `veriflow/llm/human_annotation.py` - Human annotation hooks
- `veriflow/schema.py` - Added HumanAnnotationConfig

## Decisions Made

- Annotation format: JSON files with test cases and outputs
- Integration: Human annotations merged with automated evaluation results
- Configuration: Optional, can be enabled/disabled via config

## Issues Encountered

None

## Next Step

Ready for 04-06-PLAN.md (Integration with CLI and config)

</output>
