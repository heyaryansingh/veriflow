---
phase: 04-llm-rag-evaluation
plan: 02
type: execute
depends_on: []
files_modified: [veriflow/llm/dataset_replay.py]
---

<objective>
Implement dataset replay for consistent LLM evaluation.

Purpose: Replay evaluation datasets consistently across runs. This ensures LLM evaluations are reproducible and comparable. Dataset replay loads test cases and executes them deterministically.
Output: Dataset replay module that loads and executes evaluation datasets for LLM evaluation.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dataset replay module</name>
  <files>veriflow/llm/dataset_replay.py</files>
  <action>Create `veriflow/llm/dataset_replay.py` with:
- `EvaluationTestCase` dataclass: Contains input (str), expected_output (str | dict | None), metadata (dict)
- `load_evaluation_dataset(path: Path) -> list[EvaluationTestCase]`: Loads evaluation dataset from JSON/JSONL file
- `replay_dataset(dataset: list[EvaluationTestCase], llm_func: Callable) -> list[dict]`: Replays dataset through LLM function:
  - Calls llm_func(input) for each test case
  - Returns list of dicts with: test_case, actual_output, passed (bool), error (str | None)
- Handle JSON and JSONL formats
- Support both string inputs and structured inputs
- Export functions from `veriflow/llm/__init__.py`</action>
  <verify>File exists, can load datasets, replay works with test LLM function</verify>
  <done>Dataset replay module created</done>
</task>

<task type="auto">
  <name>Task 2: Add dataset validation and error handling</name>
  <files>veriflow/llm/dataset_replay.py</files>
  <action>Add validation and error handling:
- `validate_dataset(dataset: list[EvaluationTestCase]) -> dict`: Validates dataset structure, returns validation result
- Handle missing inputs, malformed JSON, empty datasets gracefully
- Add retry logic for LLM calls (optional, configurable)
- Add timeout handling for LLM calls
- Clear error messages for dataset loading failures</action>
  <verify>Validation works, error handling graceful, retry/timeout logic works</verify>
  <done>Dataset validation and error handling implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test dataset replay with sample data</name>
  <files></files>
  <action>Test dataset replay:
- Create test JSON dataset with evaluation test cases
- Create mock LLM function that returns predictable outputs
- Replay dataset through mock LLM, verify outputs captured correctly
- Test with JSONL format
- Test error handling (malformed dataset, LLM errors)
- Clean up test files after verification</action>
  <verify>Dataset replay works correctly, handles errors gracefully, supports JSON/JSONL</verify>
  <done>Dataset replay tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/llm/dataset_replay.py` exists with dataset replay functions
- [ ] Can load datasets from JSON/JSONL
- [ ] Dataset replay executes test cases correctly
- [ ] Error handling works gracefully
- [ ] Functions exported from `veriflow/llm/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Dataset replay implemented
- Supports JSON and JSONL formats
- Error handling works
- Dataset replay tested

</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-rag-evaluation/04-02-SUMMARY.md`:

# Phase 4 Plan 2: Dataset Replay Summary

**Implemented dataset replay for consistent LLM evaluation**

## Accomplishments

- Created dataset replay module with EvaluationTestCase support
- Implemented dataset loading from JSON/JSONL formats
- Added validation and error handling

## Files Created/Modified

- `veriflow/llm/dataset_replay.py` - Dataset replay implementation
- `veriflow/llm/__init__.py` - Added dataset replay exports

## Decisions Made

- Dataset format: JSON/JSONL with input, expected_output, metadata fields
- Replay: Executes test cases through LLM function, captures outputs
- Error handling: Graceful handling of LLM errors, retry logic optional

## Issues Encountered

None

## Next Step

Ready for 04-03-PLAN.md (Judge model support)

</output>
