---
phase: 04-llm-rag-evaluation
plan: 01
type: execute
depends_on: []
files_modified: [veriflow/llm/__init__.py, veriflow/llm/prompt_hashing.py]
---

<objective>
Implement prompt/version hashing for tracking prompt changes.

Purpose: Create deterministic hashes of prompts and prompt versions to detect when prompts change. This is critical for LLM evaluation reproducibility - prompt changes invalidate comparisons.
Output: Prompt hashing module that generates deterministic hashes for prompts and tracks prompt versions.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompt hashing module</name>
  <files>veriflow/llm/__init__.py, veriflow/llm/prompt_hashing.py</files>
  <action>Create `veriflow/llm/` directory and `veriflow/llm/prompt_hashing.py` module with:
- `hash_prompt(prompt: str, version: str | None = None) -> str`: Computes SHA256 hash of prompt (and optional version string)
- `PromptVersion` dataclass: Contains prompt (str), version (str), hash (str), timestamp (datetime)
- `create_prompt_version(prompt: str, version: str | None = None) -> PromptVersion`: Creates PromptVersion with hash
- `prompt_version_to_dict(pv: PromptVersion) -> dict`: Converts to dict for JSON serialization
- `prompt_version_from_dict(data: dict) -> PromptVersion`: Creates from dict
- Use hashlib.sha256 for deterministic hashing
- Handle empty prompts gracefully
- Export functions from `veriflow/llm/__init__.py`</action>
  <verify>File exists, can hash prompts, same prompt produces same hash, version affects hash</verify>
  <done>Prompt hashing module created</done>
</task>

<task type="auto">
  <name>Task 2: Add prompt version storage</name>
  <files>veriflow/llm/prompt_hashing.py</files>
  <action>Add functions for prompt version management:
- `save_prompt_version(pv: PromptVersion, path: Path) -> None`: Saves prompt version to JSON file in `.veriflow/prompts/` directory
- `load_prompt_version(path: Path) -> PromptVersion | None`: Loads prompt version from JSON, returns None if not found
- `list_prompt_versions() -> list[PromptVersion]`: Lists all saved prompt versions
- Create `.veriflow/prompts/` directory if needed
- Use JSON format for storage, include timestamp</action>
  <verify>Functions exist, prompt versions save/load correctly, listing works</verify>
  <done>Prompt version storage implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test prompt hashing and versioning</name>
  <files></files>
  <action>Test prompt hashing:
- Create test prompt, hash it, verify deterministic (same prompt = same hash)
- Create prompt with version, verify version affects hash
- Save prompt version, load it, verify data preserved
- Test with different prompts, verify different hashes
- Test with empty prompt, verify handled gracefully
- Clean up test files after verification</action>
  <verify>Prompt hashing works correctly, versioning works, storage/loading works</verify>
  <done>Prompt hashing and versioning tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/llm/prompt_hashing.py` exists with prompt hashing functions
- [ ] Prompt hashing is deterministic (same prompt = same hash)
- [ ] Prompt versions save/load correctly
- [ ] Functions exported from `veriflow/llm/__init__.py`
</verification>

<success_criteria>

- All tasks completed
- Prompt hashing implemented
- Prompt version storage works
- Deterministic hashing verified

</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-rag-evaluation/04-01-SUMMARY.md`:

# Phase 4 Plan 1: Prompt Hashing Summary

**Implemented prompt/version hashing for tracking prompt changes**

## Accomplishments

- Created prompt hashing module with deterministic SHA256 hashing
- Implemented prompt version storage and loading
- Added prompt version management functions

## Files Created/Modified

- `veriflow/llm/__init__.py` - LLM module initialization
- `veriflow/llm/prompt_hashing.py` - Prompt hashing implementation

## Decisions Made

- Hashing: SHA256 for deterministic prompt hashes
- Version tracking: Optional version string affects hash
- Storage: JSON files in .veriflow/prompts/ directory

## Issues Encountered

None

## Next Step

Ready for 04-02-PLAN.md (Dataset replay)

</output>
