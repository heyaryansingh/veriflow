---
phase: 01-foundation
plan: 03
type: execute
depends_on: ["01-02"]
files_modified: [veriflow/config.py, veriflow/schema.py]
---

<objective>
Implement config parsing and validation for `veriflow.yaml`.

Purpose: Create the config system that reads and validates `veriflow.yaml` files, providing sensible defaults and type-safe validation using Pydantic.
Output: Config module that can load and validate YAML config files with schema validation.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pydantic config schema models</name>
  <files>veriflow/schema.py</files>
  <action>Create `veriflow/schema.py` with Pydantic models for config validation. Based on PROJECT.md requirements, create models:
- `ProjectConfig`: name (str), type (Literal["llm_classification", "tabular", "vision", "rag"])
- `BaselineConfig`: ref (str, default="main")
- `DataChecksConfig`: checks (list[str])
- `ModelConfig`: framework (Literal["sklearn", "pytorch", "hf", "api"]), deterministic (bool, default=True)
- `EvaluationConfig`: metrics (dict), statistical_tests (dict)
- `UIConfig`: enabled (bool), runner (str), base_url (str), start (dict), contracts (dict)
- `GatesConfig`: fail_on_regression (bool), fail_on_schema_change (bool)
- `VeriflowConfig`: Root model containing all above sections with defaults
Use Pydantic v2 syntax, provide sensible defaults matching PROJECT.md requirements. Make fields optional where appropriate with defaults.</action>
  <verify>File `veriflow/schema.py` exists, can import models, `VeriflowConfig()` creates instance with defaults</verify>
  <done>Config schema models created with type validation</done>
</task>

<task type="auto">
  <name>Task 2: Create config loading module</name>
  <files>veriflow/config.py</files>
  <action>Create `veriflow/config.py` with:
- `load_config(path: str | Path) -> VeriflowConfig`: Loads YAML file, parses with PyYAML, validates with Pydantic schema, returns VeriflowConfig instance
- `find_config_file(start_dir: Path = Path.cwd()) -> Path | None`: Searches up directory tree for `veriflow.yaml` or `.veriflow.yaml`
- `get_config() -> VeriflowConfig`: Convenience function that finds and loads config, raises FileNotFoundError if not found
- Handle YAML parsing errors, Pydantic validation errors with clear messages
- Use pathlib.Path for path handling
Import from `veriflow.schema import VeriflowConfig`.</action>
  <verify>File `veriflow/config.py` exists, `load_config("test.yaml")` loads and validates YAML, errors are clear</verify>
  <done>Config loading module created with YAML parsing and validation</done>
</task>

<task type="auto">
  <name>Task 3: Create example veriflow.yaml template</name>
  <files>veriflow.yaml.example</files>
  <action>Create `veriflow.yaml.example` file with a complete example config matching the schema. Include all sections from PROJECT.md:
- project (name, type)
- baseline (ref)
- data.checks (list)
- model (framework, deterministic)
- evaluation (metrics, statistical_tests)
- ui (enabled, runner, base_url, start, contracts)
- gates (fail_on_regression, fail_on_schema_change)
Use YAML comments to explain each section. This will be used by `veriflow init` in next plan.</action>
  <verify>File `veriflow.yaml.example` exists, `load_config("veriflow.yaml.example")` validates successfully</verify>
  <done>Example config template created and validates</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/schema.py` exists with Pydantic models
- [ ] `veriflow/config.py` exists with load_config function
- [ ] `veriflow.yaml.example` exists and validates
- [ ] `load_config("veriflow.yaml.example")` returns valid VeriflowConfig instance
- [ ] Invalid YAML or schema violations produce clear error messages
</verification>

<success_criteria>

- All tasks completed
- Config schema models created with Pydantic
- Config loading with YAML parsing works
- Example config file validates correctly
- Error handling provides clear messages

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Config Parsing Summary

**Implemented config parsing and validation for veriflow.yaml**

## Accomplishments

- Created Pydantic schema models for config validation
- Implemented YAML config loading with validation
- Created example config template (veriflow.yaml.example)

## Files Created/Modified

- `veriflow/schema.py` - Pydantic models for config schema
- `veriflow/config.py` - Config loading and validation logic
- `veriflow.yaml.example` - Example config template

## Decisions Made

- Config validation: Pydantic v2 (type-safe, clear errors)
- Config format: YAML (human-readable, supports comments)
- Config discovery: Search up directory tree for veriflow.yaml

## Issues Encountered

None

## Next Step

Ready for 01-04-PLAN.md (Project structure initialization)

</output>
