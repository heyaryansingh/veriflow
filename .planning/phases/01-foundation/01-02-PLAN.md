---
phase: 01-foundation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [veriflow/cli.py, veriflow/__main__.py]
---

<objective>
Create CLI entry point and command skeleton for Veriflow.

Purpose: Establish the CLI interface with three core commands (init, run, compare) as stubs that will be implemented in later phases.
Output: Working CLI entry point with command structure, commands respond with placeholder messages.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI module with command structure</name>
  <files>veriflow/cli.py</files>
  <action>Create `veriflow/cli.py` using Click framework. Structure:
- Import click
- Create main Click group: `@click.group()` decorator, `def cli()` function
- Add three commands as placeholders:
  1. `@cli.command()` `def init()` - prints "veriflow init: Initialize project (not yet implemented)"
  2. `@cli.command()` `def run()` - prints "veriflow run: Run verification checks (not yet implemented)"
  3. `@cli.command()` `def compare()` - prints "veriflow compare: Compare against baseline (not yet implemented)"
- Export `main()` function that calls `cli()` for entry point
Follow Click best practices: use decorators, clear command names, prepare for future arguments.</action>
  <verify>File `veriflow/cli.py` exists, `python -m veriflow.cli --help` shows three commands (init, run, compare)</verify>
  <done>CLI module created with three command stubs</done>
</task>

<task type="auto">
  <name>Task 2: Create __main__.py entry point</name>
  <files>veriflow/__main__.py</files>
  <action>Create `veriflow/__main__.py` that allows running via `python -m veriflow`. Import and call `main()` from `veriflow.cli`. This enables `python -m veriflow` as an alternative to the `veriflow` console script.</action>
  <verify>File `veriflow/__main__.py` exists, `python -m veriflow --help` works and shows same commands as console script</verify>
  <done>__main__.py created, package can be run as module</done>
</task>

<task type="auto">
  <name>Task 3: Test CLI installation and commands</name>
  <files></files>
  <action>Install package in development mode: `pip install -e .`. Then test:
- `veriflow --help` shows help text with three commands
- `veriflow init` prints placeholder message
- `veriflow run` prints placeholder message  
- `veriflow compare` prints placeholder message
Verify entry point from pyproject.toml works correctly.</action>
  <verify>`veriflow --help` shows three commands, each command prints its placeholder message</verify>
  <done>CLI entry point works, all commands respond correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/cli.py` exists with Click group and three commands
- [ ] `veriflow/__main__.py` exists and calls CLI main
- [ ] `pip install -e .` succeeds
- [ ] `veriflow --help` shows three commands
- [ ] Each command (init, run, compare) executes and prints placeholder message
</verification>

<success_criteria>

- All tasks completed
- CLI entry point functional
- Three commands (init, run, compare) respond with placeholders
- Package installs correctly
- Entry point from pyproject.toml works

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: CLI Entry Point Summary

**Created CLI entry point with command skeleton (init, run, compare)**

## Accomplishments

- Created `veriflow/cli.py` with Click framework
- Implemented three command stubs: init, run, compare
- Created `veriflow/__main__.py` for module execution
- Verified CLI installation and entry point works

## Files Created/Modified

- `veriflow/cli.py` - CLI module with command structure
- `veriflow/__main__.py` - Module entry point

## Decisions Made

- CLI framework: Click (standard, well-documented)
- Command structure: Group with subcommands (init, run, compare)
- Entry point: Both console script and `python -m veriflow` supported

## Issues Encountered

None

## Next Step

Ready for 01-03-PLAN.md (Config parsing and validation)

</output>
