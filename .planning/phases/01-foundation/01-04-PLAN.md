---
phase: 01-foundation
plan: 04
type: execute
depends_on: ["01-03"]
files_modified: [veriflow/cli.py, veriflow/init.py]
---

<objective>
Implement `veriflow init` command and project structure creation.

Purpose: Create the `veriflow init` command that sets up project structure (directories and initial config file) as specified in PROJECT.md.
Output: Working `veriflow init` command that creates directory structure and initial `veriflow.yaml` config file.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create init module with directory structure logic</name>
  <files>veriflow/init.py</files>
  <action>Create `veriflow/init.py` with function `create_project_structure(base_path: Path = Path.cwd()) -> None`:
- Create directories from PROJECT.md: `data_checks/`, `evals/`, `ui_checks/e2e/`, `ui_checks/contracts/`, `baselines/`, `artifacts/`, `reports/`, `.veriflow/`
- Create empty `__init__.py` files in Python directories (data_checks, evals) for future modules
- Create `.gitkeep` files in empty directories (baselines, artifacts, reports, .veriflow) so they're tracked in git
- Use pathlib.Path, handle existing directories gracefully (don't error, just skip)
- Print created directories to console</action>
  <verify>File `veriflow/init.py` exists, function creates all required directories, handles existing dirs gracefully</verify>
  <done>Init module created with directory structure logic</done>
</task>

<task type="auto">
  <name>Task 2: Implement veriflow init command</name>
  <files>veriflow/cli.py</files>
  <action>Update `veriflow/cli.py` `init()` command:
- Import `create_project_structure` from `veriflow.init`
- Import `load_config`, `Path` from `veriflow.config`
- Check if `veriflow.yaml` already exists in current directory
- If exists: Ask user to confirm overwrite (use Click's `click.confirm()`)
- If not exists or confirmed: Copy `veriflow.yaml.example` to `veriflow.yaml` (or create from template)
- Call `create_project_structure()` to create directories
- Print success message with next steps
Use Click's `@click.option()` for optional flags like `--force` to skip confirmation.</action>
  <verify>Updated `veriflow/cli.py`, `veriflow init` creates directories and config file, handles existing config gracefully</verify>
  <done>Init command implemented and functional</done>
</task>

<task type="auto">
  <name>Task 3: Test veriflow init end-to-end</name>
  <files></files>
  <action>Test `veriflow init` command:
- Run `veriflow init` in a test directory
- Verify all directories are created (data_checks/, evals/, ui_checks/, baselines/, artifacts/, reports/, .veriflow/)
- Verify `veriflow.yaml` is created from example template
- Verify `veriflow.yaml` validates with `load_config()`
- Test `veriflow init` again (should prompt about existing config)
- Test `veriflow init --force` (should overwrite without prompt)
Clean up test directory after verification.</action>
  <verify>`veriflow init` creates complete project structure, config file validates, handles existing config correctly</verify>
  <done>Init command tested and working end-to-end</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/init.py` exists with directory creation logic
- [ ] `veriflow init` command creates all required directories
- [ ] `veriflow init` creates `veriflow.yaml` from template
- [ ] Created config file validates correctly
- [ ] Command handles existing config gracefully
- [ ] All directories from PROJECT.md are created
</verification>

<success_criteria>

- All tasks completed
- `veriflow init` command functional
- Project structure created correctly
- Config file generated and validates
- Command handles edge cases (existing config, existing directories)

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`:

# Phase 1 Plan 4: Project Structure Initialization Summary

**Implemented veriflow init command with project structure creation**

## Accomplishments

- Created `veriflow/init.py` with directory structure logic
- Implemented `veriflow init` command that creates directories and config file
- Tested end-to-end initialization flow

## Files Created/Modified

- `veriflow/init.py` - Directory structure creation logic
- `veriflow/cli.py` - Updated init command implementation

## Decisions Made

- Directory structure: Matches PROJECT.md specification exactly
- Config generation: Copy from example template (veriflow.yaml.example)
- Existing config handling: Prompt for confirmation, --force flag to skip

## Issues Encountered

None

## Next Step

Phase 1 complete. Ready for Phase 2: Data Verification

</output>
