---
phase: 02-data-verification
plan: 05
type: execute
depends_on: ["02-01", "02-03"]
files_modified: [veriflow/data/drift.py]
---

<objective>
Implement distribution delta detection vs baseline.

Purpose: Compare current dataset distributions against baseline to detect drift. Uses simple statistical heuristics (mean, std, value counts) to identify significant distribution changes.
Output: Drift detection module that compares distributions and reports significant deltas.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-verification/02-01-SUMMARY.md
@.planning/phases/02-data-verification/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create distribution comparison functions</name>
  <files>veriflow/data/drift.py</files>
  <action>Create `veriflow/data/drift.py` with:
- `compute_distribution_stats(df: pd.DataFrame, column: str) -> dict`: Computes distribution statistics for a column:
  - For numeric: mean, std, min, max, percentiles (25, 50, 75)
  - For categorical: value_counts (top 10), unique_count
- `compare_distributions(stats1: dict, stats2: dict, threshold: float = 0.1) -> dict`: Compares two distribution stats:
  - For numeric: computes relative difference in mean/std (abs(mean1 - mean2) / mean1)
  - For categorical: computes KL divergence or chi-square-like metric
  - Returns dict with: changed (bool), delta (float), details (str)
  - threshold: relative change threshold (default 10%)
- Handle missing columns, different column types gracefully</action>
  <verify>File exists, distribution stats computed correctly, comparison detects significant changes</verify>
  <done>Distribution comparison functions created</done>
</task>

<task type="auto">
  <name>Task 2: Implement baseline comparison function</name>
  <files>veriflow/data/drift.py</files>
  <action>Add `check_drift_vs_baseline(current_path: str | Path, baseline_path: str | Path, threshold: float = 0.1, columns: list[str] | None = None) -> dict`:
- Loads current and baseline datasets
- If columns provided: checks only those columns; otherwise checks all common columns
- Computes distribution stats for each column in both datasets
- Compares distributions, identifies columns with drift
- Returns dict with: passed (bool), drifted_columns (list of dicts with column name and delta details), summary (str)
- Uses fingerprinting to check if datasets are identical (skip drift check if same)
- Clear error messages for missing files or incompatible datasets</action>
  <verify>Function exists, detects drift correctly, reports drifted columns with details</verify>
  <done>Baseline comparison function implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test drift detection with distribution changes</name>
  <files></files>
  <action>Test drift detection:
- Create baseline dataset with normal distribution
- Create current dataset with shifted mean (should detect drift)
- Create current dataset with same distribution (should pass)
- Create current dataset with different categorical distribution (should detect drift)
- Test with threshold parameter (adjust threshold, verify sensitivity)
- Clean up test files after verification</action>
  <verify>Drift detection works for numeric and categorical columns, respects threshold, reports accurate deltas</verify>
  <done>Drift detection tested and working correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/data/drift.py` exists with distribution comparison functions
- [ ] Distribution stats computed correctly for numeric and categorical columns
- [ ] Drift detection identifies significant distribution changes
- [ ] Baseline comparison works correctly
- [ ] Test cases verify drift detection accuracy
</verification>

<success_criteria>

- All tasks completed
- Distribution comparison implemented
- Drift detection vs baseline works
- Detects significant distribution changes
- Clear reporting of drifted columns

</success_criteria>

<output>
After completion, create `.planning/phases/02-data-verification/02-05-SUMMARY.md`:

# Phase 2 Plan 5: Distribution Drift Summary

**Implemented distribution delta detection vs baseline**

## Accomplishments

- Created distribution statistics computation functions
- Implemented distribution comparison with threshold detection
- Added baseline comparison function for drift detection

## Files Created/Modified

- `veriflow/data/drift.py` - Drift detection implementation

## Decisions Made

- Distribution stats: Mean/std for numeric, value_counts for categorical
- Drift threshold: Default 10% relative change (configurable)
- Comparison: Relative difference for numeric, KL divergence for categorical

## Issues Encountered

None

## Next Step

Ready for 02-06-PLAN.md (Integration with CLI and config)

</output>
