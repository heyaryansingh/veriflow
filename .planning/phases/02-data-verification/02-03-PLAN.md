---
phase: 02-data-verification
plan: 03
type: execute
depends_on: ["02-01"]
files_modified: [veriflow/data/fingerprint.py]
---

<objective>
Implement dataset fingerprinting for change detection.

Purpose: Create deterministic fingerprints (hashes) of datasets to detect when data changes. Fingerprints include row count, column hash, and content hash for reproducible change detection.
Output: Fingerprinting module that generates deterministic hashes for datasets.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-verification/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dataset fingerprinting functions</name>
  <files>veriflow/data/fingerprint.py</files>
  <action>Create `veriflow/data/fingerprint.py` with:
- `DatasetFingerprint` dataclass: row_count (int), column_hash (str), content_hash (str), timestamp (datetime)
- `compute_fingerprint(df: pd.DataFrame) -> DatasetFingerprint`: Computes fingerprint from DataFrame:
  - row_count: len(df)
  - column_hash: SHA256 hash of sorted column names
  - content_hash: SHA256 hash of sorted row hashes (hash each row, sort hashes, hash the sorted list)
- `fingerprint_to_dict(fp: DatasetFingerprint) -> dict`: Converts to dict for JSON serialization
- `fingerprint_from_dict(data: dict) -> DatasetFingerprint`: Creates from dict
- Use hashlib.sha256 for hashing, ensure deterministic (sort columns/rows consistently)
- Handle empty DataFrames gracefully</action>
  <verify>File exists, compute_fingerprint generates deterministic hashes, same DataFrame produces same fingerprint</verify>
  <done>Fingerprinting functions created</done>
</task>

<task type="auto">
  <name>Task 2: Implement fingerprint storage and comparison</name>
  <files>veriflow/data/fingerprint.py</files>
  <action>Add functions for fingerprint management:
- `save_fingerprint(path: Path, fingerprint: DatasetFingerprint) -> None`: Saves fingerprint to JSON file in `.veriflow/fingerprints/` directory
- `load_fingerprint(path: Path) -> DatasetFingerprint | None`: Loads fingerprint from JSON, returns None if not found
- `compare_fingerprints(fp1: DatasetFingerprint, fp2: DatasetFingerprint) -> dict`: Compares two fingerprints, returns dict with: changed (bool), row_count_diff (int), column_changed (bool), content_changed (bool), details (str)
- Create `.veriflow/fingerprints/` directory if needed
- Use JSON format for storage, include timestamp</action>
  <verify>Functions exist, fingerprints save/load correctly, comparison detects changes accurately</verify>
  <done>Fingerprint storage and comparison implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test fingerprinting with dataset changes</name>
  <files></files>
  <action>Test fingerprinting detects changes:
- Create test CSV, compute fingerprint, save it
- Modify test CSV (add row, change value, add column), compute new fingerprint
- Compare fingerprints, verify changes detected correctly
- Test that identical datasets produce identical fingerprints
- Test that fingerprint persists across saves/loads
- Clean up test files after verification</action>
  <verify>Fingerprinting detects row count changes, column changes, and content changes correctly</verify>
  <done>Fingerprinting tested and working correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/data/fingerprint.py` exists with DatasetFingerprint class
- [ ] Fingerprint computation is deterministic (same data = same hash)
- [ ] Fingerprints save/load correctly
- [ ] Comparison detects all change types (rows, columns, content)
- [ ] Test cases verify fingerprinting works correctly
</verification>

<success_criteria>

- All tasks completed
- Dataset fingerprinting implemented
- Fingerprints are deterministic and reproducible
- Storage and comparison functions work
- Change detection verified with tests

</success_criteria>

<output>
After completion, create `.planning/phases/02-data-verification/02-03-SUMMARY.md`:

# Phase 2 Plan 3: Dataset Fingerprinting Summary

**Implemented dataset fingerprinting for change detection**

## Accomplishments

- Created DatasetFingerprint class with row_count, column_hash, content_hash
- Implemented deterministic fingerprint computation
- Added fingerprint storage and comparison functions

## Files Created/Modified

- `veriflow/data/fingerprint.py` - Fingerprinting implementation

## Decisions Made

- Fingerprint components: Row count, column hash, content hash
- Hashing: SHA256 for deterministic hashes
- Storage: JSON files in .veriflow/fingerprints/ directory
- Comparison: Detects row count changes, column changes, content changes

## Issues Encountered

None

## Next Step

Ready for 02-04-PLAN.md (Overlap detection)

</output>
