---
phase: 02-data-verification
plan: 06
type: execute
depends_on: ["02-02", "02-03", "02-04", "02-05"]
files_modified: [veriflow/data/checks.py, veriflow/cli.py, veriflow/schema.py]
---

<objective>
Integrate data verification checks into CLI and config system.

Purpose: Wire up all data checks (schema, fingerprinting, overlap, drift) into a unified verification system accessible via CLI and config. This completes Phase 2 by making data checks executable.
Output: Integrated data checks module that runs all checks based on config, accessible via `veriflow run`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-verification/02-02-SUMMARY.md
@.planning/phases/02-data-verification/02-03-SUMMARY.md
@.planning/phases/02-data-verification/02-04-SUMMARY.md
@.planning/phases/02-data-verification/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified data checks orchestrator</name>
  <files>veriflow/data/checks.py</files>
  <action>Create `veriflow/data/checks.py` with:
- `run_data_checks(config: VeriflowConfig, dataset_paths: dict[str, str | Path] | None = None) -> dict`: Orchestrates all data checks:
  - Reads config.data.checks list to determine which checks to run
  - Runs checks: schema_consistency, no_train_eval_overlap, drift_vs_baseline
  - Returns dict with: passed (bool), results (dict mapping check name to result), errors (list), summary (str)
- `check_schema_consistency()`: Wrapper that calls schema.py functions
- `check_no_overlap()`: Wrapper that calls overlap.py functions
- `check_drift()`: Wrapper that calls drift.py functions, uses baseline.ref from config
- Handles missing datasets, config errors gracefully
- Export from `veriflow/data/__init__.py`</action>
  <verify>File exists, run_data_checks executes all enabled checks, returns unified results</verify>
  <done>Data checks orchestrator created</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI run command to execute data checks</name>
  <files>veriflow/cli.py</files>
  <action>Update `veriflow/cli.py` `run()` command:
- Import `get_config` from `veriflow.config`
- Import `run_data_checks` from `veriflow.data.checks`
- Load config using `get_config()`
- Check if config.data.checks is non-empty
- If enabled: Call `run_data_checks(config)` with appropriate dataset paths
- Print results: passed/failed, summary of each check, errors if any
- Exit with non-zero code if checks fail
- If no data checks configured: Print message that data checks are not configured
- Keep placeholder for other check types (model eval, UI checks) - will be implemented in later phases</action>
  <verify>Updated CLI, `veriflow run` executes data checks, prints results clearly, exits correctly on failure</verify>
  <done>CLI run command integrated with data checks</done>
</task>

<task type="auto">
  <name>Task 3: Test end-to-end data verification flow</name>
  <files></files>
  <action>Test complete data verification flow:
- Create test veriflow.yaml with data.checks configured
- Create test datasets (train.csv, eval.csv) with known issues (overlap, schema mismatch)
- Run `veriflow run` - should detect issues and fail
- Fix issues in datasets, run again - should pass
- Test with different check combinations (enable/disable checks in config)
- Verify error messages are clear and actionable
- Clean up test files after verification</action>
  <verify>End-to-end flow works, detects issues correctly, passes when data is valid, config controls which checks run</verify>
  <done>Data verification flow tested end-to-end</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/data/checks.py` exists with run_data_checks function
- [ ] CLI run command executes data checks
- [ ] Config controls which checks are enabled
- [ ] Results printed clearly with pass/fail status
- [ ] End-to-end flow works with test datasets
</verification>

<success_criteria>

- All tasks completed
- Data checks orchestrator functional
- CLI run command integrated
- Config controls check execution
- End-to-end verification flow works

</success_criteria>

<output>
After completion, create `.planning/phases/02-data-verification/02-06-SUMMARY.md`:

# Phase 2 Plan 6: Data Checks Integration Summary

**Integrated data verification checks into CLI and config system**

## Accomplishments

- Created unified data checks orchestrator
- Updated CLI run command to execute data checks
- Tested end-to-end verification flow

## Files Created/Modified

- `veriflow/data/checks.py` - Data checks orchestrator
- `veriflow/cli.py` - Updated run command

## Decisions Made

- Check orchestration: Single function that runs all enabled checks
- Config-driven: Checks enabled/disabled via config.data.checks list
- Error handling: Continues running checks even if one fails, reports all results

## Issues Encountered

None

## Next Step

Phase 2 complete. Ready for Phase 3: Classic ML Evaluation

</output>
