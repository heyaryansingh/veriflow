---
phase: 02-data-verification
plan: 02
type: execute
depends_on: ["02-01"]
files_modified: [veriflow/data/schema.py]
---

<objective>
Implement schema consistency checks for datasets.

Purpose: Verify that datasets maintain consistent schema (columns, types, nullability) across splits and versions. This catches schema drift before it breaks downstream code.
Output: Schema checking module that validates column names, types, and nullability constraints.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-verification/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema representation and comparison</name>
  <files>veriflow/data/schema.py</files>
  <action>Create `veriflow/data/schema.py` with:
- `DatasetSchema` class: Represents schema with columns (dict mapping column name to type), nullable columns (set), and row_count (int)
- `extract_schema(df: pd.DataFrame) -> DatasetSchema`: Extracts schema from DataFrame (column names, dtypes, nullability)
- `compare_schemas(schema1: DatasetSchema, schema2: DatasetSchema) -> dict`: Compares two schemas, returns dict with keys: missing_columns, extra_columns, type_changes, nullable_changes
- `validate_schema_consistency(schemas: list[DatasetSchema]) -> dict`: Validates multiple schemas are consistent, returns pass/fail with details
- Use pandas dtypes for type checking, handle nullable types (Int64 vs int64)
- Clear error messages for schema mismatches</action>
  <verify>File exists, can extract schema from DataFrame, compare_schemas detects differences correctly</verify>
  <done>Schema representation and comparison functions created</done>
</task>

<task type="auto">
  <name>Task 2: Implement schema consistency check function</name>
  <files>veriflow/data/schema.py</files>
  <action>Add `check_schema_consistency(dataset_paths: list[str | Path], reference_schema: DatasetSchema | None = None) -> dict`:
- Loads each dataset, extracts schema
- If reference_schema provided: compares each against reference
- If no reference: compares all schemas against first one
- Returns dict with: passed (bool), errors (list of dicts with dataset path and mismatch details), summary (str)
- Handles file loading errors gracefully
- Export from `veriflow/data/__init__.py`</action>
  <verify>Function exists, detects schema inconsistencies correctly, returns clear error messages</verify>
  <done>Schema consistency check function implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test schema consistency checks</name>
  <files></files>
  <action>Create test datasets and verify schema checking:
- Create test CSV files: one with columns (A, B, C), another with (A, B, D) - should detect missing/extra columns
- Create test files with same columns but different types (int vs float) - should detect type changes
- Create test files with nullable vs non-nullable columns - should detect nullable changes
- Run `check_schema_consistency()` on test files, verify it catches all mismatches
- Clean up test files after verification</action>
  <verify>Schema checks detect column mismatches, type changes, and nullable differences correctly</verify>
  <done>Schema consistency checks tested and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/data/schema.py` exists with DatasetSchema class
- [ ] Schema extraction and comparison functions work
- [ ] Schema consistency check detects mismatches correctly
- [ ] Error messages are clear and actionable
- [ ] Test cases verify all mismatch types are detected
</verification>

<success_criteria>

- All tasks completed
- Schema representation and comparison implemented
- Schema consistency checks detect all mismatch types
- Clear error messages for schema violations
- Functions tested with various schema differences

</success_criteria>

<output>
After completion, create `.planning/phases/02-data-verification/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Schema Consistency Summary

**Implemented schema consistency checks for datasets**

## Accomplishments

- Created DatasetSchema class for schema representation
- Implemented schema extraction and comparison functions
- Added schema consistency check that detects column, type, and nullable mismatches

## Files Created/Modified

- `veriflow/data/schema.py` - Schema checking implementation

## Decisions Made

- Schema representation: Column names, types, nullability
- Comparison: Detects missing columns, extra columns, type changes, nullable changes
- Reference schema: Optional - if provided, compares against it; otherwise compares all against first

## Issues Encountered

None

## Next Step

Ready for 02-03-PLAN.md (Dataset fingerprinting)

</output>
