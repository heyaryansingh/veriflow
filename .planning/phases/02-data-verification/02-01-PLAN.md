---
phase: 02-data-verification
plan: 01
type: execute
depends_on: []
files_modified: [pyproject.toml, veriflow/data/__init__.py, veriflow/data/loader.py]
---

<objective>
Add data processing dependencies and create base data loading utilities.

Purpose: Establish foundation for data verification by adding pandas/numpy dependencies and creating utilities to load datasets from common formats (CSV, Parquet, JSON).
Output: Dependencies added, data loading module created with functions to load datasets and extract basic metadata.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data processing dependencies</name>
  <files>pyproject.toml</files>
  <action>Update `pyproject.toml` to add dependencies for data processing:
- pandas>=2.0.0 (dataframe operations, CSV/Parquet reading)
- numpy>=1.24.0 (numerical operations, array handling)
- pyarrow>=12.0.0 (Parquet file support)
Add these to the main `dependencies` list (not optional). These are core requirements for data verification.</action>
  <verify>File `pyproject.toml` updated, dependencies section includes pandas, numpy, pyarrow</verify>
  <done>Dependencies added to pyproject.toml</done>
</task>

<task type="auto">
  <name>Task 2: Create data loading module</name>
  <files>veriflow/data/__init__.py, veriflow/data/loader.py</files>
  <action>Create `veriflow/data/` directory and `veriflow/data/loader.py` module with:
- `load_dataset(path: str | Path) -> pd.DataFrame`: Loads CSV, Parquet, or JSON files, returns pandas DataFrame
- `get_dataset_info(df: pd.DataFrame) -> dict`: Returns metadata dict with keys: row_count, column_count, column_names, dtypes, memory_usage
- `detect_file_format(path: Path) -> str`: Detects file format from extension (csv, parquet, json)
- Handle common errors (file not found, invalid format, encoding issues)
- Use pandas for CSV/Parquet, json module for JSON (convert to DataFrame)
- Export functions from `veriflow/data/__init__.py`</action>
  <verify>Files exist, can import functions, `load_dataset()` loads test CSV successfully</verify>
  <done>Data loading module created with dataset loading and info extraction</done>
</task>

<task type="auto">
  <name>Task 3: Install dependencies and verify</name>
  <files></files>
  <action>Install updated dependencies: `pip install -e .`. Then verify:
- `import pandas as pd` works
- `import numpy as np` works
- `from veriflow.data import load_dataset, get_dataset_info` works
- Create a test CSV file, load it with `load_dataset()`, verify `get_dataset_info()` returns correct metadata
Clean up test file after verification.</action>
  <verify>Dependencies install successfully, data loading functions work with test file</verify>
  <done>Dependencies installed and data loading verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pyproject.toml` includes pandas, numpy, pyarrow dependencies
- [ ] `veriflow/data/loader.py` exists with load_dataset and get_dataset_info functions
- [ ] `pip install -e .` succeeds
- [ ] Data loading functions work with test CSV file
- [ ] Dataset info extraction returns correct metadata
</verification>

<success_criteria>

- All tasks completed
- Data processing dependencies added
- Data loading module functional
- Can load datasets from CSV/Parquet/JSON
- Dataset metadata extraction works

</success_criteria>

<output>
After completion, create `.planning/phases/02-data-verification/02-01-SUMMARY.md`:

# Phase 2 Plan 1: Data Dependencies Summary

**Added data processing dependencies and base data loading utilities**

## Accomplishments

- Added pandas, numpy, pyarrow to dependencies
- Created veriflow/data/loader.py with dataset loading functions
- Implemented dataset info extraction (row count, columns, dtypes)

## Files Created/Modified

- `pyproject.toml` - Added data processing dependencies
- `veriflow/data/__init__.py` - Data module initialization
- `veriflow/data/loader.py` - Dataset loading and metadata extraction

## Decisions Made

- Data processing: pandas (standard DataFrame operations)
- File formats: CSV, Parquet, JSON support
- Metadata extraction: Row count, column info, dtypes

## Issues Encountered

None

## Next Step

Ready for 02-02-PLAN.md (Schema consistency checks)

</output>
