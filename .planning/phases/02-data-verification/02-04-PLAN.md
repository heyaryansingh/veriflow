---
phase: 02-data-verification
plan: 04
type: execute
depends_on: ["02-01"]
files_modified: [veriflow/data/overlap.py]
---

<objective>
Implement exact overlap detection between dataset splits.

Purpose: Detect when training and evaluation datasets share rows (data leakage). This is critical for ML correctness - overlap invalidates evaluation results.
Output: Overlap detection module that identifies exact duplicate rows between datasets.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-verification/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create overlap detection functions</name>
  <files>veriflow/data/overlap.py</files>
  <action>Create `veriflow/data/overlap.py` with:
- `find_overlap(df1: pd.DataFrame, df2: pd.DataFrame, key_columns: list[str] | None = None) -> dict`: Finds exact overlap between two DataFrames:
  - If key_columns provided: uses those columns as keys for comparison
  - If None: uses all columns (full row comparison)
  - Returns dict with: overlap_count (int), overlap_rows (pd.DataFrame), overlap_percentage (float), details (str)
- `check_train_eval_overlap(train_path: str | Path, eval_path: str | Path, key_columns: list[str] | None = None) -> dict`: Convenience function that loads datasets and checks overlap
- Handle empty datasets, mismatched columns gracefully
- Use pandas merge or set operations for efficient overlap detection
- Clear error messages if datasets can't be compared</action>
  <verify>File exists, find_overlap detects duplicate rows correctly, handles edge cases (empty, no overlap, full overlap)</verify>
  <done>Overlap detection functions created</done>
</task>

<task type="auto">
  <name>Task 2: Implement multi-split overlap checking</name>
  <files>veriflow/data/overlap.py</files>
  <action>Add `check_all_splits_overlap(split_paths: dict[str, str | Path], key_columns: list[str] | None = None) -> dict`:
- Takes dict mapping split names to paths (e.g., {"train": "train.csv", "eval": "eval.csv", "test": "test.csv"})
- Checks overlap between all pairs of splits
- Returns dict with: passed (bool), overlaps (list of dicts with split pairs and overlap details), summary (str)
- Only reports overlaps found (empty list if no overlaps)
- Handles missing files gracefully</action>
  <verify>Function exists, checks all split pairs correctly, reports overlaps clearly</verify>
  <done>Multi-split overlap checking implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test overlap detection with various scenarios</name>
  <files></files>
  <action>Test overlap detection:
- Create test datasets: train.csv and eval.csv with no overlap - should pass
- Create datasets with 5 overlapping rows - should detect and report
- Create datasets with same rows but different order - should detect overlap
- Test with key_columns (e.g., ID column) - should use keys for comparison
- Test with empty datasets - should handle gracefully
- Clean up test files after verification</action>
  <verify>Overlap detection works for all scenarios, reports accurate overlap counts and percentages</verify>
  <done>Overlap detection tested and working correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `veriflow/data/overlap.py` exists with overlap detection functions
- [ ] Overlap detection works for two datasets
- [ ] Multi-split checking works for all pairs
- [ ] Key columns option works correctly
- [ ] Test cases verify overlap detection accuracy
</verification>

<success_criteria>

- All tasks completed
- Overlap detection implemented
- Detects exact duplicate rows between datasets
- Multi-split checking works correctly
- Clear reporting of overlap details

</success_criteria>

<output>
After completion, create `.planning/phases/02-data-verification/02-04-SUMMARY.md`:

# Phase 2 Plan 4: Overlap Detection Summary

**Implemented exact overlap detection between dataset splits**

## Accomplishments

- Created overlap detection functions for two datasets
- Implemented multi-split overlap checking
- Added support for key column comparison

## Files Created/Modified

- `veriflow/data/overlap.py` - Overlap detection implementation

## Decisions Made

- Overlap detection: Exact row matching (all columns or key columns)
- Key columns: Optional - if provided, uses those columns for comparison
- Reporting: Overlap count, percentage, and sample rows

## Issues Encountered

None

## Next Step

Ready for 02-05-PLAN.md (Distribution deltas vs baseline)

</output>
